name: s07_action_06

on:
  push:
    branches: ["main"]

env:
  mi_Nombre: "Romel Cardenas Javier"

jobs:
    mensaje:
        runs-on: ubuntu-latest

        steps:

            - name: "Mensaje variable de entorno"
              run: echo "Hello ${{ env.mi_Nombre }}"